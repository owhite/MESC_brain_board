# Teensy USB access for non-root uploads.
# Allow all PJRC vendor IDs so both runtime USB and bootloader USB are accessible.
SUBSYSTEM=="usb", ATTR{idVendor}=="16c0", MODE:="0664", GROUP:="plugdev", TAG+="uaccess"
